# F4.3 — Certificación Funcional Integral de F4

- **Fecha**: 2026-08-18 (UTC-6)
- **Agente responsable**: Antigravity
- **Rama autorizada**: `desarrollo`
- **Baseline de inicio F4.3**: `d08093684f244334dc5461d76dfbf183d0a80ec7`
- **PR #20**: Permanence **OPEN / DRAFT / NOT MERGED**
- **Rama `main`**: Intacta (`727082c6fcf90f95ce6db5eadf5c4b152397d080`)
- **Base de Datos Oracle**: 0 ejecuciones DDL/DML, 0 scripts Oracle ejecutados, 0 cambios de esquema o datos en Oracle. Sí se realizó un ajuste de SQL embebido en C# para mantener la semántica BORRADOR del filtro.
- **Fase 5 (F5)**: NO INICIADA

---

## 1. Objetivos Cumplidos en F4.3

1. **Sincronización Git**:
   - Fast-forward a `origin/desarrollo` en el baseline `d08093684f244334dc5461d76dfbf183d0a80ec7`.
2. **Semántica BORRADOR en Backend**:
   - Ajuste en `MatricesRiesgosRepository.cs` en `whereSql`: `AND NVL(f.FLU_ESTADO, 'BORRADOR') = :estado`. Esto garantiza coherencia del 100% entre la proyección del SELECT (`NVL(f.FLU_ESTADO, 'BORRADOR') AS ESTADO`) y la cláusula WHERE de filtrado por estado.
3. **Suite de Pruebas Integrales F4.3 (`matrices-riesgos.component.f4.3.spec.ts`)**:
   - 16 pruebas integrales en Angular que verifican:
     - **Carga inicial**: 1 sola consulta con filtros vacíos y page-size 10 (sin prefetch de 200).
     - **DOM Superior**: Presencia de `#filtro-buscar`, `#filtro-estado` y `#filtro-registros-por-pagina` con opciones `[10, 20, 50]`. Deshabilitado durante carga.
     - **Flujo integral**: Preservación de contexto (`buscar`, `estado`, `pagina`, `registrosPorPagina`).
     - **Resets**: Reinicio automático a `pagina = 1` al cambiar búsqueda, estado o page-size.
     - **Debounce + Estado**: 1 sola consulta combinada al escribir y cambiar estado antes de 300 ms.
     - **Paginador DOM**: Botones Anterior/Siguiente, números compactos, ellipsis `…`, `aria-current="page"` y `aria-label="Ir a la página N"`.
     - **0 Registros & Una página**: Mensaje "Mostrando 0 – 0 de 0 registros" y botones deshabilitados.
     - **Última página parcial**: Coherencia en "Mostrando 21 – 23 de 23 registros".
     - **Página fuera de rango**: Adopción automática de `paginaEfectiva` de backend.
     - **Concurrencia tardía**: Descarte seguro de respuestas tardías (SUCCESS y ERROR) mediante monotonic sequence guard.
     - **Retry & KPIs**: Preservación de contexto y coherencia del indicador Total Evaluaciones.
     - **Acciones (Ver, Editar BORRADOR, Seguimiento)**: Apertura de modales con `evaId` exacto y retención de contexto F4 al cerrar.
     - **Recarga post edición / transición**: Reutilización de filtros y estado actual.

---

## 2. Resultados de Pruebas y Quality Gates

- **Backend Tests (.NET C#)**: **409 / 409 PASS (100%)**
- **Frontend Unit Tests (Angular Vitest)**: **335 / 335 PASS (100%)** en 34 archivos spec (incluye F3, F4.1, F4.2 y F4.3)
- **Frontend Build (`npm run build`)**: **PASS** (`dist/rl-app`)
- **Frontend E2E (Playwright)**: **14 / 14 PASS (100%)**
- **Formato Git (`git diff --check`)**: **PASS** (0 advertencias/errores)
- **Quality Gates Script (`run_quality_gates.ps1`)**: **SUCCESS (Puertas de calidad correctas)**

---

## 3. Estado del Repositorio
- **Rama**: `desarrollo`
- **Quality Gates**: SUCCESS
- **Sonar**: Diferido al cierre global
- **Fase 5 (F5)**: NO INICIADA
