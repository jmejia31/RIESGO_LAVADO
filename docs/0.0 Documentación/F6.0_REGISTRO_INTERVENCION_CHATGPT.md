# Registro de intervención — ChatGPT — F6.0 Auditoría del contrato

- **Fecha:** 2026-08-19, America/Tegucigalpa.
- **Agente:** ChatGPT.
- **Rama:** `desarrollo`.
- **Baseline inicial:** `7692c5fd14c3058b17b6245ca596b931ac844009`.
- **Commit concurrente preservado:** `c2b8b44c2da27aaa24e3d5ec54ff55228ebdd43f` (`docs(matrices): auditar contrato JSON y catalogos para F6.0`).
- **Objetivo:** auditar contrato JSON/catálogos completo antes de cualquier corrección productiva F6.1.
- **Código productivo modificado:** 0 archivos.
- **Oracle:** 0 DDL, 0 DML, 0 scripts manuales.
- **`main`:** no modificada.
- **PR #20:** debe permanecer OPEN / DRAFT / NOT MERGED.

## Trabajo realizado

1. Se verificó el contrato funcional v3 del usuario (`CONTRATO_FORMULARIO_DINAMICO`, v1.0.0, `LISTO_PARA_IMPLEMENTACION`) y su reporte de validación 8/8.
2. Se auditó el contrato ejecutable frontend: modelos, normalizador, Form Builder, serializador y validador.
3. Se auditó backend: `FormularioValidador`, DTO/proyección de metodología, AppService y persistencia de `VER_JSON`/`EVA_DATOS_JSON`.
4. Se contrastó el fixture SQL legacy basado en `id` con la escritura canónica actual basada en `clave`.
5. Se preservó el documento F6.0 publicado concurrentemente y se agregó un anexo de causalidad ejecutable en vez de sobrescribirlo.
6. Se materializó un fixture mínimo con forma v3 para las pruebas contractuales de F6.1.
7. Se consolidó el punto de continuidad en `ESTADO_COLABORACION.md`.

## Hallazgos críticos

- Builder `clave` vs backend `id`.
- `catalogo-multiple` `string[]` vs validación backend `Int32`.
- DTO de metodología pierde metadata necesaria para el renderer.
- contrato v3 `catalogos` como objeto rico vs runtime reducido `CatalogoMatrices[]`.
- Form Builder ofrece catálogos hardcodeados en lugar de usar la versión real.
- respuesta v3 estructurada/envelope excede el mapa reducido de respuestas F5.

## Decisión

**F6.0 = COMPLETA.**

La siguiente subfase exclusiva es **F6.1 — Normalización, validación y alineación contractual**. F6.2 permanece bloqueada hasta que F6.1 convierta las brechas en pruebas y correcciones verificadas.

## Validaciones de esta intervención

F6.0 es documental/auditoría y no altera código productivo. No se ejecutó Oracle ni se ejecutaron suites locales desde ChatGPT. El HEAD documental publicado deberá obtener Quality Gates remoto SUCCESS antes del handoff definitivo a F6.1.
