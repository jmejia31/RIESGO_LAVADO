# F6.3 — Handoff técnico ChatGPT → AntiG

## Estado

F6.3 permanece **EN PROGRESO**. La base técnica backend/API de ChatGPT está implementada y certificada; falta integración frontend, verificación post-save, reapertura y certificación runtime/navegador por AntiG antes del cierre formal.

## Baseline

- Baseline certificado F6.2: `f3b3057a78f0444960f40b975584ab344345d2dd`.
- Rama autorizada: `desarrollo`.
- PR rector: #20 `desarrollo -> main`, OPEN / DRAFT / NOT MERGED.
- `main`: `727082c6fcf90f95ce6db5eadf5c4b152397d080`.
- Usuario QA oficial único vigente: `cuentajavier419@gmail.com`.

## Base técnica implementada por ChatGPT

### Commit API

`07629509a25670f0f7289baafea8b36080eb5fb3` — `feat(matrices): exponer version de formulario por id para reapertura`

Se agregó `MatricesRiesgosFormulariosController` con el endpoint seguro de lectura:

`GET /api/matrices-riesgos/formularios/{id}`

El endpoint reutiliza `IMatricesRiesgosAppService.ObtenerVersionFormularioAsync(id)`, que ya existía, y permite recuperar exactamente la versión concreta que acaba de guardarse o que se desea reabrir. No agrega tablas, migraciones ni DML manual.

### Commit de pruebas

`5bd040177ffaf35ffa40697fd99eaf95ecb37714` — `test(matrices): certificar contrato bidireccional de versiones de formulario`

Suite nueva: `MatricesRiesgosFormularioRoundTripTests.cs`.

Casos cubiertos:

1. lectura por ID conserva el `VerJson` rico sin pérdida semántica;
2. actualización de borrador propaga al repositorio el JSON completo con extensiones futuras, `false`, `0`, `null`, `001` y `G-IVM`;
3. JSON inválido no llega al repositorio;
4. endpoint GET delega el ID exacto y responde 200;
5. versión inexistente responde 404.

## Evidencia CI de la base técnica

- Quality Gates: **#1142**.
- Run ID: **`32383879562`**.
- Head SHA certificado: `5bd040177ffaf35ffa40697fd99eaf95ecb37714`.
- Resultado: **SUCCESS**.
- Job: `96473467657`.
- Pasos de calidad, build, tests, cobertura, E2E y contenedores: SUCCESS.

## Persistencia backend verificada

La ruta de escritura vigente se conserva:

`PUT /api/matrices-riesgos/formularios/{id}` → `ActualizarBorradorFormularioAsync` → `MatricesRiesgosRepository.ActualizarBorradorFormularioAsync`.

El repositorio persiste `VER_JSON` como CLOB y `VER_HASH`, y restringe el UPDATE a versiones `DRAFT` con `VER_VIGENTE = 0`. La clonación reutiliza `VerJson` de la versión origen. F6.3 no introduce DDL ni cambios de esquema.

## Trabajo pendiente de AntiG

AntiG debe completar exclusivamente la superficie frontend/runtime de F6.3:

1. agregar en `MatricesRiesgosService` la lectura tipada `obtenerVersionFormulario(id)` contra `GET /formularios/{id}`;
2. al abrir el Builder, obtener por ID la versión autoritativa y cargar su `verJson`, en vez de depender únicamente de una copia del historial;
3. después de `actualizarBorradorFormulario`, volver a leer el mismo `verId` y comprobar equivalencia semántica entre el JSON enviado y el JSON persistido;
4. ante discrepancia, no declarar éxito silencioso ni perder el JSON enviado: mantener contexto, informar el error y permitir recuperación/reintento;
5. cerrar/reabrir el Builder y demostrar que el mismo DRAFT conserva catálogos, elementos, referencias, metadatos lossless, `0`, `false`, `null`, `001` y `G-IVM`;
6. añadir pruebas frontend por responsabilidad funcional para servicio y componente;
7. ejecutar suite completa, build, E2E y Quality Gates;
8. certificar navegador real usando únicamente `cuentajavier419@gmail.com`; la contraseña se introduce manualmente y nunca se documenta;
9. actualizar bitácora, estado y dossier final F6.3;
10. certificar también el HEAD documental final.

## Restricciones

- No tocar `main`.
- No fusionar/cerrar PR #20.
- No crear ramas.
- No ejecutar SQL, DDL, DML ni scripts Oracle manualmente.
- No tocar `B10_*`.
- No bajar cobertura ni Quality Gates.
- No eliminar, omitir o saltar E2E.
- No modificar Sonar para forzar verde.
- No exponer secretos.
- **No iniciar F6.4.**

## Gate de cierre

F6.3 solo puede cerrarse cuando el flujo real quede demostrado:

`abrir DRAFT → editar → serializar → PUT → VER_JSON → GET mismo verId → equivalencia semántica → cerrar → reabrir → misma definición sin pérdida`.
