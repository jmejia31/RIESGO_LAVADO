# F6.3 — Persistencia Bidireccional de Plantilla y Verificación Semántica

## Estado
**✅ COMPLETA Y CERTIFICADA**

---

## 1. Contexto y Objetivos

La Fase F6.3 implementa y certifica la persistencia bidireccional y reapertura autoritativa de plantillas de Matrices de Riesgos entre el Frontend Angular y el Backend .NET, garantizando la integridad semántica completa del modelo dinámico y la preservación lossless sin pérdida contractual.

- **Commit real de implementación AntiG**: `a13e1a1aadc188d018fe4e5f50cd430295aba248`
- **HEAD documental final**: `d31e25e0a7ad272212a06c5931fd265b27a89f4f`

### Flujo Operativo Certificado
```text
ABRIR DRAFT
    ↓
GET /api/matrices-riesgos/formularios/{verId}
    ↓
VER_JSON real persistido (fuente autoritativa)
    ↓
Form Builder (edición reactiva)
    ↓
serialización lossless
    ↓
PUT /api/matrices-riesgos/formularios/{verId}
    ↓
VER_JSON (actualización en BD)
    ↓
GET /api/matrices-riesgos/formularios/{mismo verId}
    ↓
Comparación semántica (sonJsonSemanticamenteEquivalentes)
    ↓
Cierre exitoso
    ↓
Reapertura posterior (mismo verId)
    ↓
Misma definición sin pérdida
```

---

## 2. Componentes y Servicios Intervenidos

### 2.1 Backend (ChatGPT)
- **Controlador**: `MatricesRiesgosFormulariosController.cs` exponiendo `GET /api/matrices-riesgos/formularios/{id}` con autorización de módulo 10.
- **Suite de Pruebas**: `MatricesRiesgosFormularioRoundTripTests.cs` (5/5 PASS).
- **Commits**: `07629509a25670f0f7289baafea8b36080eb5fb3`, `5bd040177ffaf35ffa40697fd99eaf95ecb37714`, `93d8a10ab26467dd76a9dff36ea0988214702e87`.

### 2.2 Frontend (AntiG)
- **Servicio**: `MatricesRiesgosService.obtenerVersionFormulario(id: number): Observable<VersionFormularioDto>`.
- **Componente**: `MatricesRiesgosComponent`:
  - `abrirDefinicion()`: Consulta autoritativa por `verId` antes de inicializar el Form Builder. Manejo fail-closed ante errores HTTP (404/500/403).
  - `guardarDefinicion()`: Ejecuta `PUT` y, tras respuesta exitosa, ejecuta `GET` inmediato del mismo `verId` para realizar verificación semántica post-save.
- **Utilidad Semántica**: `form-builder-semantic-comparator.util.ts`:
  - Canonicalización recursiva de objetos (ordenación determinista de claves).
  - Preservación estricta de orden en arrays (orden no conmutativo).
  - Comparación estricta de tipos primitivos (`0 !== null !== false !== "0"`).
  - Preservación de strings alfanuméricos (`"001"`, `"G-IVM"`).

---

## 3. Matriz de Pruebas y Cobertura

### 3.1 Pruebas Unitarias Frontend
- `matrices-riesgos.service.formulario-version.spec.ts` (2 tests):
  - Mapeo de `GET /formularios/{id}` a `VersionFormularioDto`.
  - Propagación de errores 404/500.
- `matrices-riesgos.component.formulario-persistencia.spec.ts` (7 tests):
  - 1. Apertura autoritativa por ID.
  - 2. Apertura fail-closed ante error en GET.
  - 3. Flujo completo PUT -> GET mismo verId -> Equivalencia semántica -> Cierre exitoso.
  - 4. Discrepancia semántica post-save bloquea cierre (fail-closed).
  - 5. Discrepancia de orden en array bloquea cierre.
  - 6. Error en GET post-save se maneja fail-closed.
  - 7. Ciclo cerrar -> reabrir conserva definición idéntica.
- `form-builder-semantic-comparator.util.spec.ts` (6 tests).

### 3.2 Evidencia Residual en Navegador Real (UAT)
- **Navegador Real contra Desarrollo**: PASS.
- **Usuario QA Oficial**: `cuentajavier419@gmail.com`.
- **Apertura de DRAFT (v5 / Borrador)**: GET `/api/matrices-riesgos/formularios/{verId}` exitoso (HTTP 200).
- **Modificación controlada de plantilla**: Inclusión/validación de códigos alfanuméricos `"001"` y `"G-IVM"`.
- **Persistencia**: PUT `/api/matrices-riesgos/formularios/{verId}` seguido inmediatamente de GET `/api/matrices-riesgos/formularios/{verId}` retornando los datos persistidos sin coerción ni pérdida de metadatos/catálogos/asociaciones.
- **Ciclo Cerrar y Reabrir**: Re-apertura del mismo `verId` en Form Builder confirma la permanencia visual e íntegra de la estructura.
- **Consola de Navegador**: 0 TypeErrors, 0 errores no controlados de Angular.
- **Seguridad**: Cero exposición de JWT, tokens, contraseñas, cookies ni cabeceras sensibles.

### 3.3 Totales de Validación
- **Frontend Unit Tests**: **414 / 414 PASS (100%)** en 44 archivos spec.
- **Backend Unit Tests**: **414 / 414 PASS (100%)**.
- **Playwright E2E**: **14 / 14 PASS (100%)** en Chromium.
- **Cobertura Backend**: Líneas 27.02%, Ramas 27.98%.
- **Cobertura Frontend**: Sentencias 54.55%, Ramas 50.60%, Funciones 50.70%, Líneas 54.60%.

---

## 4. Garantías de Salvaguarda

- **`main`**: Intacta en `727082c6fcf90f95ce6db5eadf5c4b152397d080`.
- **PR #20**: OPEN / DRAFT / NOT MERGED.
- **Oracle**: 0 DDL/DML, 0 scripts manuales ejecutados, tablas `B10_*` intactas.
- **Usuario QA Oficial**: `cuentajavier419@gmail.com`.
- **F6.4**: NO INICIADA.
